[package]
name = "prompt-ferry-upstream-presets"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
description = "Official upstream base URLs for preset providers"

[dependencies]
reqwest = { version = "0.13.5", default-features = false }
